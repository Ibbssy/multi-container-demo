version: '1.0'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    networks:
      - appnet

  frontend:
    build:
      context: ./frontend
    ports:
      - "6160:6160"
    environment:
      - BACKEND_URL=http://backend:8080
    depends_on:
      - backend
    networks:
      - appnet

networks:
  appnet: